# Copy to .env.local in frontend/ for local dev, or set as env vars in Vercel.
# Must be a publicly reachable absolute URL (no trailing slash, end at /api).
NEXT_PUBLIC_API_BASE=http://localhost:8000/api
