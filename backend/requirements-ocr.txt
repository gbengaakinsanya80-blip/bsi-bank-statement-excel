# Optional OCR backends (pluggable - engine auto-degrades if unavailable).
# rapidocr + onnxruntime are the recommended default (no system binaries,
# works on Python 3.14). paddleocr + paddlepaddle may not yet support
# Python 3.14 on Windows; Tesseract additionally requires the tesseract
# binary on PATH.
rapidocr>=3.8.0
onnxruntime>=1.20.0
opencv-python>=4.10.0
paddleocr>=2.9.0
paddlepaddle>=2.6.0
pytesseract>=0.3.13
